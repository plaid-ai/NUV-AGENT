name: Legacy IQ9075 candidate entry point (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Reject legacy main-branch candidate publishing
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    permissions: {}
    steps:
      - name: Use the protected signed-tag publisher
        shell: bash
        run: |
          echo "This entry point is permanently disabled." >&2
          echo "Dispatch iq9075-candidate-trusted-publish.yml at candidate-publisher-v1." >&2
          exit 1
