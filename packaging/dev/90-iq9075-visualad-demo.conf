# Development-only demo override. Remove this one drop-in to return to the
# unchanged signed current slot. Never use this as Fleet release evidence.
[Service]
ExecStartPre=
ExecStartPre=/bin/mkdir -p /tmp/nuvion_clips/segments /tmp/nuvion_clips/clips
ExecStart=
ExecStart=/opt/nuvion-demo/20260910-visualad/src/packaging/dev/run-iq9075-visualad-demo.sh
Environment=NUVION_VISUALAD_THRESHOLD=0.0
