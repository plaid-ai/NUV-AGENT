# User-approved experimental development only; not numerical parity acceptance.
# Remove only this override to return to the previous video-only dev runtime.
[Service]
SupplementaryGroups=fastrpc
EnvironmentFile=/opt/nuvion-demo/20260910-visualad-htp/deployment.env
ExecStartPre=
ExecStartPre=/bin/mkdir -p /tmp/nuvion_clips/segments /tmp/nuvion_clips/clips
ExecStart=
ExecStart=/opt/nuvion-demo/20260910-visualad-htp/src/packaging/dev/run-iq9075-visualad-htp-demo.sh
